open corebase
open corecuda

inl main() =
    inl swap (a,b) x = if x then b,a else a,b
    inl m,n,k : int * int * int = 512, 512, 512
    inl [ma; mb; mc] = listm.map cupy.random_normal{mean=0f32; std=1} ([m, k; k, n; m, n])
    
    
    ()