// C++ backend example.

open corebase
open corecuda
open coreext

inl main() =
    inl ~x : sa 5 (option (i32 * u64)) = sam.init (fun i => Some (i, conv i))
    run fun () =>
        if rangem.threads_in_grid().from = 0 then
            console.write_ln x
            ()
    device_sync()
    0i32