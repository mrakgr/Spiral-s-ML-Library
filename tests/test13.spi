open corebase
open corecuda
open coreext
open tensorm
open transposing_loop

inl main() =
    run fun () =>
        inl x : heap (int * int * int) = heap (1,2,3)
        inl a,b,c = !x
        ()
        // console.write_ln {msg = "Hello"; tid = thread_index()}
