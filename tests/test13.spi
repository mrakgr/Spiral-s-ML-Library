open corebase
open corecuda
open coreext
open tensorm
open transposing_loop

inl main() =
    run fun () =>
        inl x : heap_refs (int * int * int) = heap_refs (1,2,3)
        if rangem.threads_in_grid().from = 0 then
            console.write_ln *x
        // console.write_ln {msg = "Hello"; tid = thread_index()}
    device_sync()
    0i32
