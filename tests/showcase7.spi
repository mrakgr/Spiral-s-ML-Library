open corebase
open corecuda
open coreext

open tensorm

inl main() =
    inl in_ : tensor (int * int) float = cupy.zeros (4,8)
    inl out = tensor_create in_.dim
    run fun () => 
        ml.primitives.map (fun x => x + 5) in_ out
    console.write_ln {in_ out}