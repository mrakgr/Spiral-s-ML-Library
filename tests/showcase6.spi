open corebase
open corecuda
open coreext

open tensorm

let add a b = a + b
inl main() =
    inl partially_applied_add = add 1i32
    inl _ = partially_applied_add 2
    run fun () =>
        inl _ = partially_applied_add 3
        ()