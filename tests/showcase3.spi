open corebase
open corecuda
open coreext

open tensorm

inl main() =
    inl t : tensor (int * int * int) {x : int; y : float; z : bool} = tensor_create (2,3,4)
    run fun () =>
        inl q = tensor_index (0,0,0) t
        ()