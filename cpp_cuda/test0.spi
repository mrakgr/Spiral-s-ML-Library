open corebase
open corecuda
open coreext

inl main() : int =
    inl ~y : int = dyn 2 * dyn 8
    run_cpp (fun x =>
        inl ~y : int = dyn 5 + dyn 3
        console.write_ln "hello"
        ()
        )
    device_sync()
    0